namespace WikiScrapper.Domain;

/// <summary>Wikipedia content language for fetches and UI display.</summary>
public enum WikiLanguage
{
    /// <summary>English Wikipedia (en.wikipedia.org).</summary>
    En,

    /// <summary>Polish Wikipedia (pl.wikipedia.org).</summary>
    Pl,
}
